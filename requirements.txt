# Use a version compatible with facenet-pytorch and transformers
numpy<2.0.0

# Core App Dependencies
streamlit
python-dotenv
langchain
langchain-google-genai
langchain-core

# Forensic Engines — pinned to the versions verified working together.
# Unpinned transformers on Streamlit Cloud resolves to an incompatible build
# and breaks the lazy import of AutoModelForAudioClassification.
torch==2.10.0
torchvision
torchaudio
transformers==5.2.0
tokenizers==0.22.2
safetensors==0.7.0
accelerate==1.12.0
facenet-pytorch
librosa
datasets

# Use headless OpenCV for cloud deployment to avoid GUI errors
opencv-python-headless
Pillow
scipy
gdown
soundfile
libsql-experimental
